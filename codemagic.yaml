workflows:
  build:
    name: Build APK
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build APK
        script: |
          npx cap sync android
          npm run build
          npx cap open android
